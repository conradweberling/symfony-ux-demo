<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        <!-- Custom Google font-->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@100;200;300;400;500;600;700;800;900&amp;display=swap" rel="stylesheet" />
        <!-- Bootstrap icons-->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css" rel="stylesheet" />

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="d-flex flex-column h-100">

        <main class="flex-shrink-0">
            <nav class="navbar navbar-expand-sm navbar-light bg-white py-3">
                <div class="container px-5">
                    <a class="navbar-brand" href="{{ path('default_index') }}"><span class="fw-bolder text-primary">Symfony UX Demo</span></a>
                    <ul class="navbar-nav ms-auto mb-2 mb-lg-0 small fw-bolder">
                        <li class="nav-item"><a class="nav-link" href="{{ path('default_index') }}">Start</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('chart_index') }}">Chart</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('review_index') }}">Review</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('contact_index') }}">Contact</a></li>
                    </ul>
                </div>
            </nav>

            <div class="col-lg-8 mx-auto p-4 py-md-5">
                {% block body %}{% endblock %}
            </div>

        </main>

        <footer class="bg-white py-4 mt-auto">
            <div class="container px-5">
                <div class="row align-items-center justify-content-between flex-column flex-sm-row">
                    <div class="col-auto"><div class="small m-0">Copyright &copy; Symfony UX Demo 2023</div></div>
                    <div class="col-auto">
                        Privacy
                        <span class="mx-1">&middot;</span>
                        Terms
                        <span class="mx-1">&middot;</span>
                        Contact
                    </div>
                </div>
            </div>
        </footer>

    </body>
</html>
